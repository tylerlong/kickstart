{
  "name": "kickstart-cli",
  "version": "0.1.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "upgrade": "yarn-upgrade-all",
    "test": "rm -rf test/new-project && mkdir test/new-project && node index.js -b test/boilerplate-project/ -o test/new-project"
  },
  "devDependencies": {
    "standard": "^10.0.3",
    "yarn-upgrade-all": "^0.2.1"
  },
  "dependencies": {
    "commander": "^2.12.2",
    "mkdirp": "^0.5.1",
    "nunjucks": "^3.0.1",
    "ramda": "^0.25.0"
  }
}
